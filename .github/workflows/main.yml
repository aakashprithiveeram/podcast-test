name: Genedrate Podcast Feeds 
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: run feed generator
        uses: aakashprithiveeram/podcast-generator@main
        
          
